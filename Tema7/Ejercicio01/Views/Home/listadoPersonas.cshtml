@model List<Ejercicio01.Models.DAL.clsPersona>
@using Ejercicio01.Models.DAL

    <table>
        <thead>
            <tr>
                <th>
                   Nombre
                </th>
                <th>
                    Apellidos
                </th>
            </tr>
       
    </thead>

   @*  @for (int i = 0; i <= Model.Count; i++)
    {
        <tr>
            <td>@Model[i].Nombre</td>
            <td>@Model[i].Apellidos</td>
        </tr>
    } *@

    @foreach (clsPersona persona in Model) {

        <tr>
            <td>@persona.Nombre</td>
            <td>@persona.Apellidos</td>
        </tr>

    }
    </table>

    @Html.ActionLink("Volver al inicio", "Index");